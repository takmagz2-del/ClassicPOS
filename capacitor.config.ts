import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.serenepangolinbeam',
  appName: 'serene-pangolin-beam',
  webDir: 'dist'
};

export default config;
